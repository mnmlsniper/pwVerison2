# Указываем операционную систему, либо другой образ
FROM mcr.microsoft.com/playwright:v1.50.1
# Копируем папку с автотестами в наш будущий образ
COPY . .
# Установить пакеты NPM
RUN npm i
# Установить браузеры и зависимости
RUN npx playwright install --with-deps
#Команда, которая будет выполняться для запуска автотестов
CMD ["npm", "run", "test"]